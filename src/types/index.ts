export type TSaleCard = {
    id:number,
    srcImg : string,
    name : string,
    sale ?: string,
    icon ?: any,
    className?: string
} 

export type TBlock = {
    classname? : string,
    content: string
  }